[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ocr-api"
version = "1.0.0"
description = "OCR API using EasyOCR"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["app"]